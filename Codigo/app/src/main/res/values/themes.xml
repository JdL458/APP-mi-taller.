<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base personalizada para tu tema -->
    <style name="Base.Theme.MiTaller" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Podés personalizar los colores acá si querés -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Tema principal que usará tu app -->
    <style name="Theme.MiTaller" parent="Base.Theme.MiTaller">
        <!-- Si querés agregar más personalizaciones, hacelo acá -->
    </style>

    <!-- Tema necesario para ZXing Scanner -->
    <style name="zxing_CaptureTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
